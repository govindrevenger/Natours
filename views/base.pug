doctype html 
html 
    head
        block head  
            meta(charset="UTF-8")
            meta(name="viewport" content="width=device-width, initial-scale=1.0")
            link(rel="stylesheet", href="/css/style.css")
            link(rel="shortcut icon" type="image/png" href="/img/favicon.png")
            link(href="https://fonts.googleapis.com/css?family=Lato:300,300i,700" rel="stylesheet")
            title Natours | #{title}

    body 
        // HEADER 
        include _header

        // CONTENT 
        block content 
            h1 This is just a placeHolder 

        // FOOTER
        include _footer

        script(src= '/js/mapbox.js')


//- why we start this URL here with a slash. It is because building a URL like this so starting only with the slash is called a relative URL
//- and what this will do is that it will then add at this piece of the URL after the host name. So, that host is right now one to seven point zero zero one but that is not necessary to specify here because again when writing a relative URL like this
//- it will then add what we have here to that host name, okay.
